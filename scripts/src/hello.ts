/**
 * @file Smoke-test script for the @workspace/scripts package.
 * @module scripts/hello
 *
 * Verifies that the scripts workspace builds and executes correctly.
 * Run via `pnpm --filter @workspace/scripts start`.
 */
console.log("Hello from @workspace/scripts");
